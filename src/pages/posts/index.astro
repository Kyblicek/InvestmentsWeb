---
import Layout from '../../layouts/Layout.astro';
import { db } from '../../server/db';

const posts = await db.post.findMany({
  where: {
    status: 'PUBLISHED',
  },
  orderBy: {
    publishedAt: 'desc',
  },
});

const formatDate = (date?: Date | null) =>
  date ? new Intl.DateTimeFormat('cs-CZ', { dateStyle: 'long' }).format(date) : 'Neznámé datum';

const truncate = (input: string, length = 180) => {
  if (input.length <= length) return input;
  return `${input.slice(0, length)}…`;
};
---
<Layout
  title="Blog | René Rypar"
  description="Aktuální posty a komentáře k investicím a realitnímu trhu."
  metaImage="/assets/IMG_6170.jpg"
>
  <section class="bg-[#f5f5f5] py-24">
    <div class="mx-auto flex w-full max-w-6xl flex-col gap-12">
      <header class="px-6 text-center">
        <p class="text-xs uppercase tracking-[0.3em] text-neutral-500">Blog</p>
        <h1 class="mt-2 font-serif text-4xl">Posty z LinkedIn</h1>
        <p class="mt-3 text-sm text-neutral-600">
          Výběr publikovaných myšlenek, komentářů a analýz. Pro více obsahu sledujte LinkedIn profil.
        </p>
      </header>

      <div class="grid gap-8 px-6 md:grid-cols-2">
        {posts.map((post) => (
          <article class="flex h-full flex-col overflow-hidden rounded-3xl border border-neutral-200 bg-white shadow-sm transition hover:shadow-md">
            {post.imageUrl && (
              <img
                src={post.imageUrl}
                alt={post.title}
                class="h-56 w-full object-cover"
                loading="lazy"
              />
            )}
            <div class="flex flex-1 flex-col gap-4 p-6">
              <div>
                <p class="text-xs uppercase tracking-[0.3em] text-neutral-500">{formatDate(post.publishedAt)}</p>
                <h2 class="mt-2 font-serif text-2xl">
                  <a href={`/posts/${post.id}`} class="transition hover:text-neutral-500">
                    {post.title}
                  </a>
                </h2>
              </div>
              <p class="text-sm text-neutral-600">{truncate(post.content)}</p>
              <div class="mt-auto flex items-center gap-3 pt-4">
                <a
                  href={`/posts/${post.id}`}
                  class="rounded-full bg-neutral-900 px-5 py-2 text-xs uppercase tracking-[0.3em] text-white transition hover:bg-neutral-700"
                >
                  Detail
                </a>
                {post.linkedinUrl && (
                  <a
                    href={post.linkedinUrl}
                    class="rounded-full border border-neutral-300 px-5 py-2 text-xs uppercase tracking-[0.3em] text-neutral-700 transition hover:border-neutral-500"
                    target="_blank"
                    rel="noopener"
                  >
                    Na LinkedIn
                  </a>
                )}
              </div>
            </div>
          </article>
        ))}
        {posts.length === 0 && (
          <div class="col-span-full rounded-3xl border border-dashed border-neutral-300 bg-white p-12 text-center text-neutral-500">
            Zatím nejsou publikované žádné posty. Sledujte LinkedIn pro aktuální novinky.
          </div>
        )}
      </div>
    </div>
  </section>
</Layout>
